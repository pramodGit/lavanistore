PWD: /etc/nginx/sites-available/main
===========
server {
    server_name lavanistore.in;

    root /var/www/lavanistore/frontend/dist;
    index index.html;
	
    location /cdn/images/ {
      alias /var/www/lavanistore/storage/images/;

      expires 30d;
      add_header Cache-Control "public, max-age=2592000";
    }

    location /files/ {
        alias /var/www/lavanistore/storage/files/;
    }

    location / {
        try_files $uri /index.html;
    }
    
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	    proxy_set_header X-Forwarded-Proto $scheme;
    }

}

===========
command to initiate HTTPS : certbot --nginx -d lavanistore.in
===========
nginx -t
systemctl reload nginx