server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Fix large cookies / headers
  client_header_buffer_size 32k;
  large_client_header_buffers 4 64k;

  location / {
    try_files $uri /index.html;
  }
}
